export type OrderItemState = 'pending' | 'processing' | 'delivered' | 'refunded' | 'cancelled' | 'completed';
export const orderItemStates = ['pending', 'processing', 'delivered', 'refunded', 'cancelled', 'completed'];
